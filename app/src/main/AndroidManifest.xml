<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mobilefintech16.chama">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Chama">
        <activity android:name=".SignUpActivity" android:theme="@style/noTitleBar"></activity>
        <activity android:name=".LoginActivity" android:theme="@style/noTitleBar"/>
        <activity
            android:name=".SplashActivity"
            android:theme="@style/noTitleBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".HomeActivity" />
        <activity android:name=".OnBoardingActivity" android:theme="@style/noTitleBar"/>
    </application>

</manifest>